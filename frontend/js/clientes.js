document.write('<script src="js/config.js"></script>');document.write('<script src="js/navigation.js"></script>');document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="css/navigation.css">');const api=window.TORKS_API_URL||'http://localhost:3000/api',token=localStorage.getItem('torksToken');if(!token)location.href='login.html';let clients=[];const money=c=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(c||0)/100),esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function render(){const search=document.querySelector('#search').value.toLowerCase(),subscription=document.querySelector('#subscription').value,rows=clients.filter(c=>(!search||`${c.name} ${c.email}`.toLowerCase().includes(search))&&(!subscription||(subscription==='none'?!c.subscriptionStatus:c.subscriptionStatus===subscription)));document.querySelector('#content').innerHTML=rows.length?`<table class="admin-table"><thead><tr><th>CLIENTE</th><th>PLANO</th><th>ASSINATURA</th><th>CRÉDITOS</th><th>PAGAMENTOS</th><th>TOTAL PAGO</th><th>CADASTRO</th></tr></thead><tbody>${rows.map(c=>`<tr><td><b>${esc(c.name)}</b><small>${esc(c.email)} · ${c.emailVerifiedAt?'Verificado':'E-mail pendente'}</small></td><td>${esc(c.planName)}</td><td><span class="status-badge">${esc(c.subscriptionStatus||'Sem assinatura')}</span></td><td>${c.creditsRemaining}</td><td>${c.paymentCount}</td><td class="money-positive">${money(c.totalPaidCents)}</td><td>${new Date(c.createdAt).toLocaleDateString('pt-BR')}</td></tr>`).join('')}</tbody></table>`:'Nenhum cliente encontrado.'}(async()=>{const r=await fetch(`${api}/admin/clients`,{headers:{Authorization:`Bearer ${token}`}}),b=await r.json();if(r.status===403)location.href='dashboard.html';if(!r.ok)throw Error(b.error||'Erro ao carregar clientes.');clients=b.data.clients;render()})().catch(e=>document.querySelector('#content').textContent=e.message);document.querySelectorAll('.report-filters input,.report-filters select').forEach(x=>x.oninput=render);

document.write('<script src="js/config.js"></script>');
const api=window.TORKS_API_URL||'http://localhost:3000/api',form=document.querySelector('form'),message=document.querySelector('#message');
form.addEventListener('submit',async event=>{event.preventDefault();const fields=Object.fromEntries(new FormData(form));let endpoint='forgot-password',payload={email:fields.email};if(form.id==='reset-form'){if(fields.password!==fields.confirmation){message.textContent='As senhas não são iguais.';return}endpoint='reset-password';payload={token:new URLSearchParams(location.search).get('token'),password:fields.password}}message.textContent='Aguarde...';try{const response=await fetch(`${api}/auth/${endpoint}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)}),body=await response.json();if(!response.ok)throw Error(body.error);form.hidden=true;message.innerHTML=`${body.data.message}${body.data.resetUrl?`<br><br><a class="button" href="${body.data.resetUrl}">Abrir link (teste local)</a>`:endpoint==='reset-password'?'<br><br><a class="button" href="login.html">Entrar</a>':''}`;}catch(error){message.textContent=error.message||'Não foi possível concluir.'}});

const router=require('express').Router(),controller=require('../controllers/billingController'),auth=require('../middleware/auth'),asyncHandler=require('../utils/asyncHandler');
router.get('/plans',asyncHandler(controller.plans));router.post('/webhook/mercado-pago',asyncHandler(controller.webhook));router.use(auth);router.get('/me',asyncHandler(controller.mine));router.post('/checkout',asyncHandler(controller.checkout));router.post('/renew',asyncHandler(controller.renew));router.post('/cancel',asyncHandler(controller.cancel));router.get('/payments/:id/receipt',asyncHandler(controller.receipt));module.exports=router;

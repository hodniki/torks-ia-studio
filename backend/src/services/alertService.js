const nodemailer=require('nodemailer');let lastAlert=0;
exports.failure=async data=>{if(!process.env.ALERT_EMAIL||!process.env.SMTP_HOST||Date.now()-lastAlert<300000)return;lastAlert=Date.now();const transport=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||587),secure:process.env.SMTP_SECURE==='true',auth:process.env.SMTP_USER?{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}:undefined});await transport.sendMail({from:process.env.EMAIL_FROM||process.env.SMTP_USER,to:process.env.ALERT_EMAIL,subject:`Alerta Torks Studio - erro ${data.status}`,text:`Falha em ${data.method} ${data.path}\nProtocolo: ${data.requestId}\nMensagem: ${data.message}`})};
